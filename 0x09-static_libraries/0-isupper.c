#include <stdio.h>
#include "main.h"

/**
 * _isupper - function to check whether a character is in uppercase
 * @c: the character
 * Return: returns 1 if uppercase, 0 if not
 */
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
