#include <stdio.h>
#include "operations.h"

/**
 * mod -function that gets the mod of two integers
 * @a: the 1st integer
 * @b: the 2nd integer
 * Return: returns the mod of the integers
 */
int mod(int a, int b)
{
	return (a % b);
}
