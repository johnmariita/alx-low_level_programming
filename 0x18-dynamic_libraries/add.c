#include <stdio.h>
#include "operations.h"

/**
 * add -function that adds up two integers
 * @a: the 1st integer
 * @b: the 2nd integer
 * Return: returns the sum of the integers
 */
int add(int a, int b)
{
	return (a + b);
}
