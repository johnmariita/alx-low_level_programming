#include <stdio.h>
#include "operations.h"

/**
 * div -function that divides two integers
 * @a: the 1st integer
 * @b: the 2nd integer
 * Return: returns the quotient of the integers
 */
int div(int a, int b)
{
	return (a / b);
}
