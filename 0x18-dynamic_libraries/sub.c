#include <stdio.h>
#include "operations.h"

/**
 * sub -function that gets the differrence of two integers
 * @a: the 1st integer
 * @b: the 2nd integer
 * Return: returns the diff of the integers
 */
int sub(int a, int b)
{
	return (a - b);
}
