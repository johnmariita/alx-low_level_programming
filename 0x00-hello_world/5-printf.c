#include <stdio.h>

/**
 * main - Entry point for the program
 *
 * This function prints a message to the console using
 * the printf function, and then returns 0 to indicate
 * that the program completed successfully.
 *
 * Return: 0 on success, nonzero value on error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

